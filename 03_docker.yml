---
- name: Setting all
  hosts: test_hosts
  become: yes

  tasks:
    - name: Clone git repository
      git:
        repo: https://github.com/RaZZario/netdata.git
        dest: /home/netdata/git
        version: master
      become: yes
      become_user: netdata